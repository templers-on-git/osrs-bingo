# osrs-bingo v2 — bugs & small changes

Bug reports and small UI/behavior tweaks that aren't new features — see `FEATURE_IDEAS.md` for the feature backlog instead.

## Open

1. **Item modal chips reorder on every click — should just toggle background color, order should stay stable.** Root cause: `handleItemChipClick` (`login.js`) calls `loadBoard()` + re-runs `openItemModal()` after every collect/uncollect, which fully re-fetches (`listItemsInSet`/`buildItemsBySet` in `admin.js`, no `.order(...)` clause) and rebuilds the modal's HTML from scratch. Postgres doesn't guarantee row order without `ORDER BY`, so each re-fetch can come back in a different order. Liel's call: don't want a re-sort-every-time band-aid — the actual fix should stop rebuilding the whole item list on every click (just toggle the clicked chip's collected/confirmed class client-side, no re-fetch/re-render of the rest) so order is naturally stable because nothing else moves. Deferred until the tile/modal layout rework, since that's the natural point to redo this rendering approach anyway rather than patching the current one twice.
2. **Auto-color tile brackets by difficulty/points** — brackets should get an automatic color on a green → yellow → orange → red gradient, scaled by point value: the lowest-point bracket in an event is green (easiest), the highest-point bracket is red (hardest), and brackets in between are interpolated along that gradient. Applies to the group-header pill and/or tile cards for that bracket. Not yet scoped: exact interpolation approach (HSL hue sweep is the obvious starting point — green ~120° down to red ~0°) and whether it's computed purely client-side from each event's min/max bracket points, or something admin-overridable later.
3. **Rename tile status labels "Open"/"Done" → "Available"/"Completed"** — cosmetic copy change to `status-badge` text in `tileCardHtml`/`progressTileCardHtml` (`login.js`). The underlying CSS classes (`.status-badge.open`/`.status-badge.done`) and `progress.completed` logic don't need to change, just the displayed label text.
4. **Hide completed tiles toggle should be a button, not a checkbox** — Liel wants the same look v1 used for its toggle-style controls: a `.btn-ghost`-style button with an `.active` state (like the View/Edit/Progress tab buttons already do), not a plain `<input type="checkbox">`. Behavior stays the same (per-pane, client-side only, see the earlier hide/show completed feature) — just swap the control's markup/style, toggling an `.active` class and the button's label text (e.g. "Hide completed" / "Show completed") instead of a checked checkbox.
